# Assets Directory

This directory contains static assets for BarPath AI:

- `banner.png`: Project banner image
- `demo.gif`: Demo animation
- `models/`: Pre-trained models directory

Place your custom YOLO models in the `models/` subdirectory.